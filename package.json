{
  "name": "magento-docs",
  "version": "1.0.0",
  "private": true,
  "description": "VuePress docs with auto-generated blog sidebar",
  "scripts": {
    "generate:sidebar": "node scripts/generate-sidebar.mjs",
    "docs:dev": "node scripts/dev-with-sidebar-watch.mjs",
    "docs:build": "npm run generate:sidebar && vuepress build docs"
  },
  "devDependencies": {
    "@vuepress/bundler-vite": "next",
    "@vuepress/theme-default": "next",
    "sass-embedded": "^1.100.0",
    "vue": "^3.5.13",
    "vuepress": "next"
  }
}
